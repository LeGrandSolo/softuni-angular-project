import { ShowInFrontDirective } from './show-in-front.directive';

describe('ShowInFrontDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInFrontDirective();
    expect(directive).toBeTruthy();
  });
});
